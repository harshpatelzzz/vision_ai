"""PoseVision core: edge pipeline, events, geometry, posture."""

# Import submodules directly, e.g. ``from core.pipeline import EdgeVisionPipeline``,
# to avoid loading heavy dependencies at package import time.
